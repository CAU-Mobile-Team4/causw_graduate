import 'package:flutter/material.dart';

class GraduateInformation extends StatelessWidget {
  const GraduateInformation({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Graduate Information"),),
    );
  }
}

